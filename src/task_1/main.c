#include <stdio.h>
#include <stdlib.h>

void numb_to_stict(int number);

int main()
{

    return 0;
}


void numb_to_stict(int number)
{

        char buf[255] = "";
        sprintf(buf, "%d", number);
}
